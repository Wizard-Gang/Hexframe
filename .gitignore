node_modules/
dist/
.wrangler/
*.tsbuildinfo
.DS_Store

# Credentials. None of these may ever be committed: the repository-root .env is the
# local source of truth. `.dev.vars` is the owner-only local Wrangler secret handoff.
.env
.env.*
!.env.example
.dev.vars
.dev.vars.*
